# Future Projects

Create one subfolder per project, for example:

`public/projects/housing-regression/`

Store:
- PDF/report
- screenshots
- preview image
- supporting public artifact

Then add one object to `data/projects.ts`.
